FROM node:22-alpine
# git is required by the catalog git-sync tests (tests/catalog/git-sync.test.ts),
# which shell out to `git init`/`git commit` to build fixture repos.
RUN apk add --no-cache git
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
CMD ["npm", "test"]
